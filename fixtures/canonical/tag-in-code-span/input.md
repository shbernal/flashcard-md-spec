# C and C sharp

## Which of these are tags?

- `#include <stdio.h>` is not a tag: it is in a code span, and it contains `<`
- `C#` is not a tag: the `#` is trailing
- #42 is not a tag: it has no non-numeric character
- #1st-declension is a tag

#grammar
